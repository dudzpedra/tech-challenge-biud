{
  "name": "@biud/dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint src/**/*.ts",
    "test": "vitest run --config vitest.config.ts",
    "build": "tsc -p tsconfig.json"
  }
}
